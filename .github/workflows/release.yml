name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn -B package -DskipTests --file pom.xml

    - name: Prepare Release Asset
      run: |
        # Extract version from tag (e.g., v0.1.0 -> 0.1.0)
        VERSION=${GITHUB_REF#refs/tags/v}
        # Find the shaded jar (it's the one without 'original-' prefix)
        JAR_PATH=$(ls target/oracle-to-excel-[0-9]*.jar | head -n 1)
        cp "$JAR_PATH" "target/oracle-to-excel-cli-${VERSION}.jar"
        cp "$JAR_PATH" "target/oracle-to-excel-cli-latest.jar"

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          target/oracle-to-excel-cli-*.jar
        generate_release_notes: true
